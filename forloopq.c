#include<stdio.h>
 int main (){
   // ++i = (pre increment )
   // i++ = (post increment )
    int i;
    for(i=0;i<=10;i++){//i++ = i=i+1 //use then increase
    printf("%d",i);    //++i =\\increase then use
    }
    return 0;
 }//decrement operator    // it can be float or character or double
 //--i (pre decrement )
 //i-- (post decrement)